pipeline:
  backend:
    image: haskell:8.4.4
    commands:
      - cd backend/
      - stack test
      - stack build
      - stack install
      - tar -cvf out.tar /root/.local/bin/main Dockerfile
    volumes:
      - '/tmp/cache_stack:/root/.stack'
